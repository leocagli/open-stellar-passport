# Node
node_modules/
dist/

# Circuit build artifacts
build/
*.r1cs
*.wasm
*.sym
*.zkey
*.ptau
witness.wtns
!build/.gitkeep
# …but keep the compiled contract WASMs (deployment artifacts + contractimport input)
!contracts/**/*.wasm
# …and the demo's circuit artifacts, so the frontend runs out of the box
!frontend/public/zk/**

# Rust / Soroban
target/
*.wasm.d
contracts/agent-passport-validator/test_snapshots/

# Frontend dev logs
frontend/vite.log

# Env / secrets
.env
.env.local
*.key
identity.json

# OS
.DS_Store
Thumbs.db

# Demo video intermediates (keep docs/demo.mp4)
docs/*.webm
frontend/video-tmp/
_vf_*.png

# Installed agent skills (not project code)
.agents/
_tts_test.wav
skills-lock.json
docs/ElevenLabs_*
